#include"Add.h"

//int Add(int left, int right);
//double Add(double left, double right);
int main()
{
	int a[5] = { 1, 2, 3, 4, 5 };

	//for (auto& e : a)
	//	e *= 2;
	for (auto e : a)
		cout << e << " ";
	system("pause");
	return 0;
}